// 太麻烦了，浪费时间，放了

#include <iostream>
using namespace std;

char a[85][85];

int main() {
    int n;
    while (cin >> n) {
        char inchar, outchar;
        cin >> inchar >> outchar;

        for (int i = 0; i < n; ++i) {
            if (i == 0 || i == n - 1) {
                
            }
        }
    }

    return 0;
}