// 表达式求值，教麻烦，pass

#include <iostream>
#include <stack>
#include <string>
using namespace std;

int main() {
    string str;

    while (getline(cin, str)) {
        // 先构造后序表达式
    }

    return 0;
}