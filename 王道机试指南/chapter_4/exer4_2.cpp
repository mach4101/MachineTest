// 单词替换

#include <iostream>
#include <string>
using namespace std;

int main() {
    string str;
    while (getline(cin, str)) {
        string oldstr, newstr;
        cin >> oldstr >> newstr;

        for (int i = 0; i < str.size(); ++i) {
            for (int j = 0; j < oldstr.size(); ++i) {
                bool flag = false;
            }
        }
    }

    return 0;
}